import { vars } from "~/styles/theme.css";
import { style } from "@vanilla-extract/css";

export const backGround = style({
    display: "flex",
    width: "100%",
    flexDirection: "column",
    background: "url(/images/back-ground.svg) no-repeat center",
});